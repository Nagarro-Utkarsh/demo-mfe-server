{
  "buildCommand": "echo skipping build",
  "installCommand": "npm install",
  "outputDirectory": ".",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "includeFiles": "{views/**,dist/**,fragments/**,controller/**,common/**,utils/**,app.ts,env.ts,fragment.config.ts,types.ts}"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
